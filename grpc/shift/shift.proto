syntax = "proto3";
package shift;
option go_package="shift-api/grpc/shift";

service Shift {
  rpc GetShift (GetShiftMessage) returns (ShiftResponse) {}
}
message GetShiftMessage {
  string target_user = 1;
}
message ShiftResponse {
  string name = 1;
  string shift = 2;
}